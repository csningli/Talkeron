#include "taskscrollarea.h"
#include<QScrollBar>

TaskScrollArea::TaskScrollArea(QWidget *)
{

}

//void TaskScrollArea::resizeEvent(QResizeEvent *event) {
//     QScrollArea::resizeEvent(event);
//     this->scrollBarWidgets(Qt::AlignBottom);
//     this->verticalScrollBar()->setValue(this->verticalScrollBar()->maximum());
//}
